const button = document.getElementById("myForm-button");
const Input = document.getElementById("myForm-input");

button.addEventListener("click", update);

function update(e) {
  e.preventDefault();
  //   console.log(Input);
}
